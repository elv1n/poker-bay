export const tableOptions = {
  radiusWidth: 300,
  radiusHeight: 250,
  border: 15,
};
export const Links = {
  game: '/game/',
};
export enum Routes {
  login = '/login',
  lobby = '/',
  game = '/game/:name',
}
